{
  "name": "lmy_tree",
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "check": "eslint . --quiet --no-warn-ignored; e=$?; tsc --noEmit --pretty; t=$?; [ $e -eq 0 ] && [ $t -eq 0 ] && echo \"\\n✨ Sweet! Everything's just dandy!\\n\""
  },
  "dependencies": {
    "tree-sitter-cli": "^0.26.3"
  }
}
